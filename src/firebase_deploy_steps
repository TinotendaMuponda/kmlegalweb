SETUP HOSTING CONFIG FILES

npm install -g firebase
-tools

firebase login

firebase init

firebase deploy  //to deploy every new change

Set the “ng build” folder to match the public folder we set up for firebase
